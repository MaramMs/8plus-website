/** @type {import('next').NextConfig} */
const nextConfig = {
 
  reactStrictMode: true,
  images: {
    domains: ['new.8plusit.com'],
  },

    images: {
      unoptimized: true
    }
   

  
}

module.exports = nextConfig
